Agentic Loop

Generator LLM produces Angular component

Validator checks:

Design token compliance

Syntax integrity

If invalid → feedback injected into second LLM call

Loop continues until valid or max retry reached

Mention:

Deterministic temperature (0.3)

Structured system prompts

Guardrails via rule-based validation
